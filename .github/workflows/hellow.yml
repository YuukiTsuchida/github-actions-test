name: Hello
on: push
jobs:
    hello_world_job:
        runs-on: ubuntu-latest      # 最新のUbuntu環境で実行
        env:
            PASS: PASSVALUE
        steps:                      # ステップ
        - run: echo "Hello, World!" # Hello, World! を出力
        - run: echo "::add-mask::${PASSVALUE}"
        - run: echo "${PASSVALUE}"